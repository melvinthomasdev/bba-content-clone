Store `true` in the variable `hasLicense`.

<codeblock language="javascript" type="exercise" testMode="fixedInput">
<code>
const hasLicense = ;
console.log(hasLicense);
</code>

<solution>
const hasLicense = true;
console.log(hasLicense);
</solution>
</codeblock>