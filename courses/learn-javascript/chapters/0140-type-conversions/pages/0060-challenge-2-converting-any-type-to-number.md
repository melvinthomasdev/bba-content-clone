Convert the variable `isAdmin`
to a number
and
display the output to the console.

For example:
```js
Input: isAdmin = true

Output: 1
```
<codeblock language="javascript" type="exercise" testMode="fixedInput">
<code>
const isAdmin = false;
console.log(isAdmin);
</code>

<solution>
const isAdmin = false;
console.log(Number(isAdmin));
</solution>
</codeblock>
