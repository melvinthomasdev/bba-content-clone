Ruby provides `.even?` method
to check if a number is even.

<Editor lang="ruby">
<code>
puts 2.even?

puts 21.even?
</code>
</Editor>

Ruby also provides `.odd?` method
to check if a number is odd.

<Editor lang="ruby">
<code>
puts 2.odd?

puts 21.odd?
</code>
</Editor>