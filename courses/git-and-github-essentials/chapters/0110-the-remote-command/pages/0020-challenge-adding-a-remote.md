Add a remote repository named `server` to your local repository.

<codeblock language="bash" type="exercise" testMode="fixedInput" matchSolutionCode="true">
  <code></code>

<solution>
git remote add server
</solution>
</codeblock>
