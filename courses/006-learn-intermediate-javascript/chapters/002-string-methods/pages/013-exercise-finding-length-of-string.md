Use the `string.length` property to find the length of the `Hello World!` string.

<Editor lang="javascript" type="exercise">
<code>
let stringLength = ;
console.log(stringLength);
</code>

<solution>
let stringLength = "Hello World!".length;
console.log(stringLength);
</solution>
</Editor>