The `includes()` method checks if a
particular string includes another
**substring**
and
then returns `true` or `false` accordingly.

<Editor lang="javascript">
<code>
let myString = "Sam loves mangoes.";
let result = myString.includes("es");
console.log(result);
</code>
</Editor>