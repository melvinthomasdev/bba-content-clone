Use the `toUpperCase()` method to convert the string `abcd` into capital letters.

<Editor lang="javascript" type="exercise">
<code>
let myString = ;
console.log(myString);
</code>

<solution>
let myString = "abcd".toUpperCase();
console.log(myString);
</solution>
</Editor>