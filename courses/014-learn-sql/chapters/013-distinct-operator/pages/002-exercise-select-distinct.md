Find all the unique age of all the students.



<Editor lang="sql" dbName="students1.db" type="exercise">
<code>
SELECT  
FROM
</code>

<solution>
SELECT DISTINCT(age)
FROM students
</solution>
</Editor>