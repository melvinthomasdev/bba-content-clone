The `map.size` method
returns the number of
elements in the Map.

<Editor lang="javascript">
<code>
let myMap = new Map([
  ["name", "Sam Smith"],
  [32, "age"],
  [false, "isAdmin"]
  ]);

let result = myMap.size
console.log(result);
</code>
</Editor>