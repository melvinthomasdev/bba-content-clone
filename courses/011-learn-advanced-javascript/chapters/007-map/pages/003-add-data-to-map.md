To add a key value pair to
the Map, use the `set` method.

<Editor lang="javascript">
<code>
let myMap = new Map();

myMap.set("name", "Sam Smith");
myMap.set("age", 32);
myMap.set("isAdmin", false);

console.log(myMap);
</code>
</Editor>