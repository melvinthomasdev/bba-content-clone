Use the `sort()` method
to sort the `names` array.

<Editor lang="javascript" type="exercise">
<code>
let names = ["Eve", "Sam", "Oliver", "Adam"];

console.log(names);
</code>

<solution>
let names = ["Eve", "Sam", "Oliver", "Adam"];
names.sort();
console.log(names);
</solution>
</Editor>