Store `true` in the variable `isAdult`.

<Editor lang="javascript" type="exercise">
<code>
let isAdult = ;
console.log(isAdult);
</code>

<solution>
let isAdult = true;
console.log(isAdult);
</solution>
</Editor>