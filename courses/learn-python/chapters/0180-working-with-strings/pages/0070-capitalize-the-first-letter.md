Python also supports capitalizing the first letter of a sentence using `capitalize()`.

<codeblock language="python" type="lesson">
<code>
text = "i am the king"
print(text.capitalize())
</code>
</codeblock>