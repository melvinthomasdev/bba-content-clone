Find all students who are enrolled in the _JavaScript_ course.



<codeblock language="sql" dbName="students1.db" type="exercise" testMode="fixedInput">
<code>
SELECT
FROM
</code>

<solution>
SELECT *
FROM students
WHERE course = 'JavaScript'
</solution>
</codeblock>