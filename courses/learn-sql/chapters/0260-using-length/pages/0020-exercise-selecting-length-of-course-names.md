Find the names of students along with the length of course name they are enrolled in.

<codeblock language="sql" dbName="students1.db" type="exercise" testMode="fixedInput">
<code>
SELECT
FROM
</code>

<solution>
SELECT name, LENGTH(course)
FROM students
</solution>
</codeblock>