Find the average of all ages and name it `averageOfAges`.



<codeblock language="sql" dbName="students1.db" type="exercise" testMode="fixedInput">
<code>
SELECT
FROM
</code>

<solution>
SELECT AVG(age) AS averageOfAges
FROM students
</solution>
</codeblock>