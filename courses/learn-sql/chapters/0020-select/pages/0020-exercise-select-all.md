Use `SELECT *` to select all the columns and all the data from the table.

<codeblock language="sql" dbName="students1.db" type="exercise" testMode="fixedInput">
<code>
SELECT
FROM
</code>

<solution>
SELECT *
FROM students
</solution>
</codeblock>