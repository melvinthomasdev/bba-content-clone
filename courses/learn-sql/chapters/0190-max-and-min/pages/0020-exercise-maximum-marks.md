Calculate the maximum marks among all the students.



<codeblock language="sql" dbName="students1.db" type="exercise" testMode="fixedInput">
<code>
SELECT
FROM
</code>

<solution>
SELECT MAX(marks)
FROM students
</solution>
</codeblock>